apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: aspm-test
          artifact-id: af6b0b26-f4dc-4bad-bc0a-1f0e20bbb50f
          target-environment: qa
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: aspm-test
          artifact-id: af6b0b26-f4dc-4bad-bc0a-1f0e20bbb50f
          target-environment: Production